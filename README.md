# ambulance-routing-system
Python CLI app where ambulance crew fills patient details on-site, system calculates GPS distances using Haversine formula, matches specialist &amp; bed availability across hospitals, ranks them by score, books a bed, and logs case to CSV.
